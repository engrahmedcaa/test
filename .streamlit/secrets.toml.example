# On Streamlit Cloud: App settings -> Secrets -> paste this in (with your real key)
# Locally, you can copy this to .streamlit/secrets.toml instead of using .env if you prefer.
XAI_API_KEY = "your_new_xai_key_here"
